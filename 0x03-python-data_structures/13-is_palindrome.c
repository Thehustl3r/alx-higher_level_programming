#include "lists.h"
/**
 * is_palindrome -  the functio that test if a list is palindrome
 * @head: pointer to the list
 * Return: 1 is palindrome elese 0
 */
int is_palindrome(listint_t **head)
{
	listint_t *current;

	current = *head;
	if (!current)
		return (0);
	return (1);
}
